<%= form_for(@user_source) do |f| %>
  <% if @user_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_source.errors.count, "error") %> prohibited this user_source from being saved:</h2>

      <ul>
      <% @user_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :available_source_id %><br />
    <%= f.number_field :available_source_id %>
  </div>
  <div class="field">
    <%= f.label :alias_name %><br />
    <%= f.text_field :alias_name %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%= f.number_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
